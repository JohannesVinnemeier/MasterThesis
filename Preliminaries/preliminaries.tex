\section{Preliminaries}

\begin{itemize}
    \item Chern and Stiefel-Whitney classes
    \item 1:1 correspondence of $\K$ vector bundles and $GL(n,\K)$ principal bundles + in particular complex line bundles and $S^1$-principal bundles ($\to$ two different constructions either as underlying principal bundle or by choosing metric)
    (\item example: underlying principal bundle of canonical line bundle over $\CP^n$ is $S^{2n+1} \to \CP^n$)
    \item 1:1 correspondence of $S^1$-principal bundles over $M$ and second cohomology classes of $M$
    \item Definition of spin/non-spin/totally-non-spin (Example: $\CP^2$ is not spin)
    \item Lemma: If $M$ contains a codimension $0$ (immersed) submanifold which is totally non-spin, then $M$ is itself totally non-spin
    \item Definieren die classifying maps $X \to B\pi_1(X)$ eine natürliche Transformation? (Für Kommutativität von dem einen Diagram)
\end{itemize}